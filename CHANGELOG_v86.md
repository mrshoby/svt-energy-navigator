# v86 — Upload Row Card Fix + Home Link + Help Text Clean

Fixuri:
- la `3 Încarcă fișierul`, fereastra dashed de upload revine la dimensiunea normală, nu micșorată;
- se compactează doar rândul/cardul mare de la punctul 3, prin padding vertical redus;
- uploadul PV revine la aceeași dimensiune normală ca uploadul principal;
- butonul `← Pagina principală` duce acum către `index.html`, nu către `testeaza-gratuit.html`;
- din helperul de la `2 Sursa datelor` a fost eliminat textul:
  `Cum preferi să încarci datele de consum electric?`;
- se păstrează textul:
  `Dacă ai fișierul orar, aceasta este varianta recomandată.`
  și hyperlinkul:
  `De unde obții fișierul?`.
