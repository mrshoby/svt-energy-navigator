# CHANGELOG v136 — TRUE v132 Invoice Baseline Restore

- Rebuilt the `Am doar factura lunară` section from the uploaded current repo state.
- Restores the intended v132-like behavior: invoice controls are shown only when `Am doar factura lunară` is selected.
- Cleans the broken invoice patches v120/v130/v131/v132 and the invalid HTML tag left by older patches.
- Keeps invoice inputs compact and separated, with labels directly above their own inputs.
- Keeps the invoice upload window in the invoice row on the right, without changing the distributor help modal.
